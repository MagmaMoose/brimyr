# Decisions (ADRs)

One file per architectural decision, written by `/adr`:
`NNNN-kebab-case-title.md` (e.g. `0001-pure-coverage-core.md`).

Load these **only** when the task touches the decision — never by default.
